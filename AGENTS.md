# 仓库协作规则

## 研究对象

本仓库的研究对象是双轴桨叶式混合机的智能设计，当前重点是基于 SolidWorks 方程式、外部参数文件和宏窗口实现参数化建模，并逐步整合性能计算、参数寻优、综合评价和系统验证。

## 强制事实边界

- 论文中的设备名称、结构、变量、公式、数值、图表、软件版本、API、实验和验证结果必须有本仓库证据或用户明确提供的来源。
- 师兄的双螺杆膨化机论文只能用于章节组织和写作形式参考，严禁迁移其对象、公式、数据、结果、系统截图或结论。
- 不得把“已规划”“拟实现”“界面占位”写成“已经实现”或“验证通过”。
- 对缺少证据的内容，先在 `docs/evidence-register.md` 标记缺口，再向用户索取材料或设计验证任务。
- “创新”应谨慎表述为有证据的工程方法、集成方式、实现特点或验证结果。

## 写作工作流

1. 修改前读取对应章节、`docs/progress.md` 和 `docs/evidence-register.md`。
2. 写作前明确本次任务是规划、起草、润色、审查还是定稿。
3. 修改正文后同步更新章节状态、证据登记和 `CHANGELOG.md`。
4. 影响研究结论的修改必须记录依据，不能只改措辞掩盖证据不足。
5. 输出 DOCX 定稿前，再执行学校格式核对、引用核验和逐页渲染检查。

## 论文修改意见日志

- 用户每次对论文内容、结构、用词、引用、证据或版式提出修改意见后，同步更新 `worklog/index.html`。
- 每条记录必须包含日期、论文位置、意见原意、处理结果、状态和关联文件；未完成事项不得标为“已落实”。
- 同一意见继续推进时更新原条目状态；研究方向或修改要求发生实质变化时新增条目，保留修改过程。
- 日志同时支持按提出时间和论文顺序查看，历史回溯条目应说明来源边界，不能伪装成逐字聊天记录。

## 用词与表达规则（用户2026-09-20确认）

- 后续所有章节的起草、润色及Word修改，优先使用机械工程领域常用、直接的词语，删减重复说明和抽象绕口的表达；例如“混合收益”改为“混合效果”，“结构尺度”改为“结构尺寸”，“参数决策层面”改为“选择参数时”。具体替换须结合上下文。
- 简化措辞必须保持技术含义、适用条件与结论强度。功率通常用W或kW表示；能耗表示一定过程消耗的能量，常用J或kWh。原文实际指功率时写“功率”或“所需功率”，实际指能量消耗时写“能耗”，不得混用。
- 同一概念使用统一术语，不为避免词语重复而随意换名。本课题既定指标“混合均匀度、功率和产量”保持一致；引用文献的原始题名不随正文措辞简化而改写。
- 每次修改主动检查上述问题，无需作者逐处指出；发现更常用的词会改变原意时，应说明区别并采用准确表述。

## Skill 路由

- 规划章节、证据映射和重写边界：`academic-paper-strategist`
- 从材料起草论文段落或重构论证：`nature-writing`
- 润色、英译和学术表达调整：`nature-polishing`
- 文献检索与参考文献核验：`nature-academic-search`、`nature-ref-verifier`
- 图表规划与论文级绘图：`nature-figure`
- 最终 DOCX 编辑与版式核验：`documents`
- 从现有草稿组装定稿：`academic-paper-composer`

调用 skill 不能替代研究证据。任何自动生成内容仍需按本文件的事实边界审查。

## 文献原文与Zotero附件（用户2026-09-17要求）

- 后续本仓库处理的论文原文，取得后必须关联到Zotero对应文献条目，不能只建元数据或只存本地文件。
- 先按DOI或题名、作者查重；已有条目直接添加子附件，不新建重复文献条目。导入前核对题名、作者及文件格式，不能将登录页、期刊书目来源或其他论文挂成原文。
- 按大纲目录保存一份可定位的本地原文，默认将PDF复制导入Zotero为存储型子附件；保留本地原文，不移动或删除源文件。跨章节通过多个集合复用同一条目，不重复导入附件。
- 导入后回读父条目键、附件键、可读文件路径及对应关系，并核对附件与本地原文内容一致；将结果同步登记到文献卡片、总表及批次记录。PDF至少核对格式、非零页数和题名，复制件核对SHA256。
- 无合法全文、格式异常、Zotero不可用或关联失败时明确登记缺口；“已下载”“本地附件已关联”“云端已同步”分别记录，不以提供导入脚本或RIS文件冒充实际关联完成。

## 导师式审查要求

直接指出逻辑跳跃、对象混淆、变量定义不清、验证不足和过度结论。不要为了迎合已有表述而保留明显错误；先解释问题，再给出可执行的修正方案。
